// =========================================
// DEFAULT TEST PROFILE
// =========================================
profile {    
    test {
        params {
          resultsDir  = "${projectDir}/results/"
          inputFile   = "${projectDir}/testdata/mydata.txt"
        }
    }
}